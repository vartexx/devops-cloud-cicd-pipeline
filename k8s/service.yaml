apiVersion: v1
kind: Service
metadata:
  name: devops-dashboard-service
  namespace: default
  labels:
    app: devops-dashboard
spec:
  type: NodePort
  selector:
    app: devops-dashboard
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30080
    protocol: TCP
    name: http
